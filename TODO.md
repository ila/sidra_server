# TODO

- TTL/WINDOW/REFRESH values: support time strings (minutes/hours/days) instead of raw integers. Currently uint8_t (max 255).
- RMV (Replicated Materialized View): implement leaderboard-style views with FULL_REFRESH strategy.
